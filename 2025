local A = function(b) return b end
local E = game
local F = E:GetService("HttpService")

local G = {85, 110, 105, 113, 117, 101, 73, 100, 101, 110, 116, 105, 102, 105, 101, 114}
local H = ""

for I = 1, #G do
    H = H .. string.char(G[I])
end

local mamaMia = F:GenerateGUID(false)

local FinalFunc = loadstring([[
    return function()
        local msg = "]] .. H .. [[: " .. "]] .. mamaMia .. [["
        print(msg)
    end
]])()

FinalFunc()
